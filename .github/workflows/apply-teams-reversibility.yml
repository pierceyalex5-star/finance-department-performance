name: Apply Teams Push and Reversible Actions

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/add-teams-push-and-reversible-actions.py'
      - '.github/workflows/apply-teams-reversibility.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply dashboard changes
        run: python scripts/add-teams-push-and-reversible-actions.py
      - name: Commit generated app changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html cloudflare-worker/src/index.js
          if git diff --cached --quiet; then
            echo "No generated app changes."
          else
            git commit -m "Add Teams handoff push and reversible workflow actions"
            git push
          fi
