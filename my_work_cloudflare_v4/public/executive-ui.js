(()=>{
const css=`
.metrics.exec{grid-template-columns:repeat(8,1fr)}
.attention.exec{grid-template-columns:repeat(3,1fr)}
.assistantitem,.commitment{border:1px solid var(--line);background:#fff;border-radius:14px;padding:11px;margin-bottom:8px}.assistantitem.new{border-left:4px solid var(--blue)}.commitment.mine{border-left:4px solid var(--amber)}.commitment.theirs{border-left:4px solid var(--green)}.execbody{font-size:11px;color:#4b5563;line-height:1.45;margin-top:5px}.exectools{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}.execgrid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.execbadge{font-size:10px;border:1px solid var(--line);border-radius:999px;padding:3px 7px;color:var(--muted)}
@media(max-width:1050px){.metrics.exec{grid-template-columns:repeat(4,1fr)}.execgrid{grid-template-columns:1fr}}
@media(max-width:720px){.metrics.exec{grid-template-columns:repeat(2,1fr)}.attention.exec{grid-template-columns:1fr 1fr}}
`;
const style=document.createElement('style');style.textContent=css;document.head.appendChild(style);
function addTab(view,label,before='tasks'){if(document.querySelector(`[data-view="${view}"]`))return;const nav=document.querySelector('.tabs');const b=document.createElement('button');b.className='tab';b.dataset.view=view;b.textContent=label;const ref=nav.querySelector(`[data-view="${before}"]`);nav.insertBefore(b,ref||null);b.onclick=()=>setView(view)}
function addView(id,html){if(document.getElementById('view-'+id))return;const s=document.createElement('section');s.className='view';s.id='view-'+id;s.innerHTML=html;document.querySelector('main').appendChild(s)}
addTab('assistant','Assistant','tasks');addTab('commitments','Commitments','projects');
addView('assistant',`<div class="execgrid"><div class="panel pad"><div class="sectionhead"><div><h3>Assistant Inbox</h3><div class="small">Forwarded email and other items that need triage or judgment.</div></div><span class="small" id="execAssistantCount"></span></div><div class="filters"><select id="execAssistantStatus"><option value="active">New + reviewed</option><option value="new">New</option><option value="reviewed">Reviewed</option><option value="accepted">Accepted</option><option value="dismissed">Dismissed</option><option value="all">All</option></select><input id="execAssistantSearch" placeholder="Search assistant items…"></div><div id="execAssistantList"></div></div><div class="panel pad"><div class="sectionhead"><h3>Assistant rules</h3></div><div class="note"><b>Capture</b><p>Clear actions can become tasks. Ambiguous items stay here for review.</p></div><div class="note"><b>Commitments</b><p>Promises are tracked separately from execution tasks.</p></div><div class="note"><b>Email</b><p>Draft replies can be prepared; sending remains an approval action.</p></div><div class="section quickrow"><button id="execTriage">AI triage</button></div></div></div>`);
addView('commitments',`<div class="panel pad"><div class="sectionhead"><div><h3>Commitments</h3><div class="small">What you promised and what others promised you.</div></div><span class="small" id="execCommitCount"></span></div><div class="filters"><select id="execCommitDirection"><option value="all">All directions</option><option value="mine">I owe</option><option value="theirs">Owed to me</option></select><select id="execCommitStatus"><option value="open">Open</option><option value="done">Done</option><option value="cancelled">Cancelled</option><option value="all">All</option></select><input id="execCommitSearch" placeholder="Search commitments or people…"></div><div id="execCommitList"></div></div>`);
const bottom=document.querySelector('.bottomnav');if(bottom&&!bottom.querySelector('[data-view="assistant"]')){const b=document.createElement('button');b.dataset.view='assistant';b.innerHTML='<b>✦</b>Assistant';b.onclick=()=>setView('assistant');bottom.insertBefore(b,bottom.children[2]||null);bottom.style.gridTemplateColumns='repeat(6,1fr)'}
const metrics=document.querySelector('.metrics');if(metrics){metrics.classList.add('exec');if(!document.getElementById('mExecAssistant'))metrics.insertAdjacentHTML('beforeend','<div class="metric"><div class="n" id="mExecAssistant">0</div><div class="l">Assistant inbox</div></div><div class="metric"><div class="n" id="mExecCommit">0</div><div class="l">Commitments due</div></div>')}
const att=document.querySelector('.attention');if(att){att.classList.add('exec');if(!document.getElementById('aExecAssistant'))att.insertAdjacentHTML('afterbegin','<button class="card" id="aExecAssistantBtn"><b id="aExecAssistant">0</b><span>assistant inbox</span></button><button class="card" id="aExecCommitBtn"><b id="aExecCommit">0</b><span>commitments due</span></button>')}
const todayAnchor=document.querySelector('#view-today .grid2.section:last-of-type');if(todayAnchor&&!document.getElementById('execTodayPanels'))todayAnchor.insertAdjacentHTML('afterend',`<div class="execgrid section" id="execTodayPanels"><div class="panel pad"><div class="sectionhead"><h3>Commitments at risk</h3><button class="mini" id="execAllCommit">All</button></div><div id="execTodayCommit"></div></div><div class="panel pad"><div class="sectionhead"><h3>Assistant attention</h3><button class="mini" id="execAllAssistant">Review</button></div><div id="execTodayAssistant"></div></div></div>`);
function dueStateExec(d){if(!d)return'';const k=localKey(d),today=localKey();if(k<today)return'overdue';if(k===today)return'today';return'future'}
function commitmentCard(c){const ds=dueStateExec(c.due_at),who=c.person_name||'Unspecified',dir=c.direction==='mine'?`I owe ${who}`:`${who} owes me`;return`<div class="commitment ${c.direction}" onclick="window.execOpenCommitment('${c.id}')"><div class="taskrow"><input class="check" type="checkbox" ${c.status==='done'?'checked':''} onclick="event.stopPropagation();window.execCompleteCommitment('${c.id}',this)" ${c.status==='done'?'disabled':''}><div class="taskmain"><div class="tasktitle">${esc(c.title)}</div>${c.details?`<div class="execbody">${esc(c.details)}</div>`:''}<div class="meta"><span class="tag ${c.direction==='mine'?'amber':'green'}">${esc(dir)}</span>${c.project_name?`<span class="tag blue">${esc(c.project_name)}</span>`:''}${c.due_at?`<span class="tag ${ds==='overdue'?'red':ds==='today'?'amber':''}">Due ${fmt(c.due_at)}</span>`:''}</div></div><button class="mini" onclick="event.stopPropagation();window.execOpenCommitment('${c.id}')">Edit</button></div></div>`}
function assistantCard(a){const ds=dueStateExec(a.suggested_due_at);return`<div class="assistantitem ${a.status==='new'?'new':''}"><div class="tasktitle">${esc(a.title)}</div>${a.summary?`<div class="execbody">${esc(a.summary)}</div>`:''}<div class="meta"><span class="tag blue">${esc(a.source_type||'assistant')}</span><span class="tag">${esc(a.status)}</span>${a.suggested_project_name?`<span class="tag blue">${esc(a.suggested_project_name)}</span>`:''}${a.suggested_person?`<span class="tag">With ${esc(a.suggested_person)}</span>`:''}${a.suggested_due_at?`<span class="tag ${ds==='overdue'?'red':ds==='today'?'amber':''}">Suggested due ${fmt(a.suggested_due_at)}</span>`:''}</div>${a.suggested_action?`<div class="execbody"><b>Suggested:</b> ${esc(a.suggested_action)}</div>`:''}${['new','reviewed'].includes(a.status)?`<div class="exectools"><button class="mini primary" onclick="window.execAcceptAssistant('${a.id}')">Accept as task</button><button class="mini" onclick="window.execReviewAssistant('${a.id}')">Mark reviewed</button><button class="mini" onclick="window.execDismissAssistant('${a.id}')">Dismiss</button><button class="mini" onclick="ask('Review assistant inbox item: ${esc(a.title).replace(/'/g,"\\'")}. Tell me whether it should be a task, commitment, note or draft reply.')">Ask AI</button></div>`:''}</div>`}
function renderExecAssistant(){const list=document.getElementById('execAssistantList');if(!list||!st)return;const s=document.getElementById('execAssistantStatus').value,q=document.getElementById('execAssistantSearch').value.toLowerCase().trim();let arr=st.assistant_inbox||[];if(s==='active')arr=arr.filter(a=>['new','reviewed'].includes(a.status));else if(s!=='all')arr=arr.filter(a=>a.status===s);if(q)arr=arr.filter(a=>[a.title,a.summary,a.suggested_action,a.suggested_person,a.suggested_project_name].join(' ').toLowerCase().includes(q));document.getElementById('execAssistantCount').textContent=`${arr.length} shown`;list.innerHTML=arr.map(assistantCard).join('')||'<div class="empty">No matching assistant items.</div>'}
function renderExecCommitments(){const list=document.getElementById('execCommitList');if(!list||!st)return;const dir=document.getElementById('execCommitDirection').value,status=document.getElementById('execCommitStatus').value,q=document.getElementById('execCommitSearch').value.toLowerCase().trim();let arr=st.commitments||[];if(dir!=='all')arr=arr.filter(c=>c.direction===dir);if(status!=='all')arr=arr.filter(c=>c.status===status);if(q)arr=arr.filter(c=>[c.title,c.details,c.person_name,c.project_name].join(' ').toLowerCase().includes(q));document.getElementById('execCommitCount').textContent=`${arr.length} shown`;list.innerHTML=arr.map(commitmentCard).join('')||'<div class="empty">No matching commitments.</div>'}
function renderExecPeople(){if(!st)return;document.querySelectorAll('.person').forEach((el,i)=>{const p=st.people?.[i];if(!p)return;const d=el.querySelector('div');if(d)d.textContent=`${p.shared||0} shared · ${p.delegated||0} delegated · ${p.waiting||0} waiting · ${p.owed_by_me||0} I owe · ${p.owed_to_me||0} owes me`})}
function renderExecToday(){if(!st)return;const openC=(st.commitments||[]).filter(c=>c.status==='open'),today=localKey(),risk=openC.filter(c=>c.due_at&&localKey(c.due_at)<=today).sort((a,b)=>new Date(a.due_at)-new Date(b.due_at)),activeA=(st.assistant_inbox||[]).filter(a=>['new','reviewed'].includes(a.status));document.getElementById('mExecAssistant').textContent=st.metrics?.assistant||0;document.getElementById('mExecCommit').textContent=st.metrics?.commitments_due||0;document.getElementById('aExecAssistant').textContent=st.metrics?.assistant||0;document.getElementById('aExecCommit').textContent=st.metrics?.commitments_due||0;document.getElementById('execTodayCommit').innerHTML=risk.slice(0,5).map(commitmentCard).join('')||'<div class="empty">No commitments due or overdue.</div>';document.getElementById('execTodayAssistant').innerHTML=activeA.slice(0,5).map(assistantCard).join('')||'<div class="empty">Assistant inbox is clear.</div>'}
const baseRender=render;render=function(){baseRender();renderExecAssistant();renderExecCommitments();renderExecToday();setTimeout(renderExecPeople,0)};
window.execAcceptAssistant=async id=>{try{await api(`/api/assistant-inbox/${id}/accept`,{method:'POST'});showToast('Accepted as task',false);await load()}catch(e){showToast(e.message,false)}};
window.execReviewAssistant=async id=>{try{await api(`/api/assistant-inbox/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({status:'reviewed'})});showToast('Marked reviewed',false);await load()}catch(e){showToast(e.message,false)}};
window.execDismissAssistant=async id=>{try{await api(`/api/assistant-inbox/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({status:'dismissed'})});showToast('Dismissed',false);await load()}catch(e){showToast(e.message,false)}};
window.execCompleteCommitment=async(id,cb)=>{try{await api(`/api/commitments/${id}/complete`,{method:'POST'});showToast('Commitment completed',true);await load()}catch(e){if(cb)cb.checked=false;showToast(e.message,false)}};
window.execOpenCommitment=id=>{const c=(st.commitments||[]).find(x=>x.id===id);if(!c)return;drawerTitle.textContent='Commitment';drawerBody.innerHTML=`<div class="formgrid"><div class="field full"><label>Commitment</label><input id="ecTitle" value="${esc(c.title)}"></div><div class="field full"><label>Details</label><textarea id="ecDetails">${esc(c.details||'')}</textarea></div><div class="field"><label>Direction</label><select id="ecDirection"><option value="mine" ${c.direction==='mine'?'selected':''}>I owe</option><option value="theirs" ${c.direction==='theirs'?'selected':''}>Owed to me</option></select></div><div class="field"><label>Person</label><input id="ecPerson" value="${esc(c.person_name||'')}"></div><div class="field"><label>Project</label><select id="ecProject"><option value="">No project</option>${(st.projects||[]).map(p=>`<option ${p.name===c.project_name?'selected':''}>${esc(p.name)}</option>`).join('')}</select></div><div class="field"><label>Due</label><input id="ecDue" type="datetime-local" value="${toLocalInput(c.due_at)}"></div><div class="field"><label>Status</label><select id="ecStatus">${['open','done','cancelled'].map(x=>`<option value="${x}" ${x===c.status?'selected':''}>${x}</option>`).join('')}</select></div></div><div class="section quickrow"><button class="btn primary" id="ecSave">Save</button><button class="btn" id="ecAsk">Ask AI</button></div>`;openDrawer();document.getElementById('ecSave').onclick=async()=>{const body={title:ecTitle.value,details:ecDetails.value||null,direction:ecDirection.value,person_name:ecPerson.value||null,project_name:ecProject.value||null,due_at:ecDue.value?new Date(ecDue.value).toISOString():null,status:ecStatus.value};try{await api(`/api/commitments/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(body)});closeDrawer();showToast('Commitment updated',true);await load()}catch(e){showToast(e.message,false)}};document.getElementById('ecAsk').onclick=()=>ask(`Review the commitment ${c.title}. Tell me whether it is at risk or needs a follow-up.`)};
const aiStatus=document.getElementById('execAssistantStatus'),aiSearch=document.getElementById('execAssistantSearch'),cDir=document.getElementById('execCommitDirection'),cStatus=document.getElementById('execCommitStatus'),cSearch=document.getElementById('execCommitSearch');aiStatus.onchange=renderExecAssistant;aiSearch.oninput=renderExecAssistant;cDir.onchange=renderExecCommitments;cStatus.onchange=renderExecCommitments;cSearch.oninput=renderExecCommitments;document.getElementById('execTriage').onclick=()=>ask('Review my assistant inbox and tell me what should become a task, commitment, draft reply, note or dismissal. Do not make ambiguous changes without asking.');document.getElementById('aExecAssistantBtn').onclick=()=>setView('assistant');document.getElementById('aExecCommitBtn').onclick=()=>setView('commitments');document.getElementById('execAllAssistant').onclick=()=>setView('assistant');document.getElementById('execAllCommit').onclick=()=>setView('commitments');
setTimeout(()=>{if(st)render();else load().catch(()=>{})},0);
})();
